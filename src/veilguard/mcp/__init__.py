"""
MCP server config protection (encrypt / rewrite JSON configs).

Roadmap: discover client configs, classify env vars, encrypt values, restore from backup.
Not required for core `init` / `scan` flows.
"""
